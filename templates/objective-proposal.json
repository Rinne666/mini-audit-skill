{
  "principal": "unauthenticated_remote_user",
  "initial_capabilities": [
    "send_http_request"
  ],
  "target_capabilities": [
    "arbitrary_code_execution",
    "read_other_tenant_data"
  ],
  "security_invariants": [
    "an anonymous user cannot obtain privileged execution capability",
    "an anonymous user cannot read another tenant's data"
  ]
}
